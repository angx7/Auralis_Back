{
  "compilerOptions": {
    /* --- Opciones Esenciales del Módulo --- */
    "target": "ES2022", // JavaScript moderno
    "module": "NodeNext", // El sistema de módulos para Node.js
    "moduleResolution": "NodeNext", // Cómo resuelve los módulos
    "esModuleInterop": true, // Compatibilidad con módulos CommonJS
    /* --- Opciones de Compilación (Aquí estaba el error) --- */
    "outDir": "./dist", // La carpeta de salida
    "rootDir": "./src", // La carpeta de entrada
    // "noEmit": true,                // ASEGÚRATE DE QUE ESTA LÍNEA ESTÉ BORRADA O EN FALSE
    /* --- Opciones de Calidad de Código --- */
    "strict": true, // Habilita todas las comprobaciones estrictas
    "verbatimModuleSyntax": true, // Fuerza a usar 'import type' (lo que ya arreglamos)
    "skipLibCheck": true, // Omite la comprobación de tipos en archivos .d.ts
    "forceConsistentCasingInFileNames": true
  },
  "include": [
    "src/**/*"
  ], // Qué carpetas debe compilar
  "exclude": [
    "node_modules"
  ] // Qué carpetas debe ignorar
}